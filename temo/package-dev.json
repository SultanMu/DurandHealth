{
  "name": "durandhealth-dev",
  "version": "1.0.0",
  "description": "Development configuration for DurandHealth",
  "scripts": {
    "dev": "cd frontend && npm install && npx vite --host 0.0.0.0 --port 5000"
  },
  "dependencies": {
    "zod": "^3.24.2"
  }
}